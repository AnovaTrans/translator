# Streamlit + starlette pinned to a verified-compatible pair. With only `>=`
# bounds every Streamlit Cloud rebuild floats to the latest of both, and a newer
# starlette made GZipResponder's `thread_minimum_size` required, which Streamlit's
# gzip middleware doesn't pass — crashing every request. (Deploy on Python 3.13;
# 3.14 has no pyarrow wheel — see .python-version.)
streamlit==1.51.0
starlette==1.3.0

anthropic>=0.40.0
openai>=1.30.0
pandas>=2.0.0,<3.0
numpy>=1.24.0
rapidfuzz>=3.5.0
lxml>=4.9.0
tenacity>=8.2.0
python-dotenv>=1.0.0

# document <-> XLIFF conversion (io/documents.py)
python-docx>=1.1.0
python-pptx>=0.6.21
openpyxl>=3.1.0
beautifulsoup4>=4.12.0

# reference-file parsing for the (optional) semantic style matcher
pdfplumber>=0.10.0
striprtf>=0.0.26
